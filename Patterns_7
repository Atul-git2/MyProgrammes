	Scanner sc = new Scanner(System.in);
		int s = sc.nextInt();
    
    for(int i = 1; i<=s; i++) {
			for(int j = 1; j<=s; j++) {
				System.out.print(i+ " "+ j + " ");
			}
			System.out.println();
		}
